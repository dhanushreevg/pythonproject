# Moving Object Detection using OpenCV

This project detects moving objects using a webcam using Python and OpenCV.

## Files
- pyex.py – main Python file for moving object detection

## Requirements
- Python 3
- OpenCV

## How to Run
1. Install Python
2. Install OpenCV:
   pip install opencv-python
3. Run the program:
   python pyex.py

## Output
The program detects moving objects and draws bounding boxes around them.
